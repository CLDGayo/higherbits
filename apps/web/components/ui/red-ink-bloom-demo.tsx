// higherbits studio scaffolds the component as src/components/ui/component.tsx,
// reachable at the fixed alias path below (NOT a slug path).
import { Component } from "./red-ink-fluid";

export default function DemoComponent() {
  return <Component />;
}
